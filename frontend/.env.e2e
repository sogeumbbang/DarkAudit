# Browser tests must not depend on a developer's local backend or .env.local settings.
VITE_USE_MOCKS=true

# 목업 스크린샷(/mock/*.png)은 dev 서버의 public/ 에서 받아야 한다.
# 이 값이 비어 있지 않으면 resolveApiUrl 이 백엔드 주소를 앞에 붙여서
# 이미지가 깨지고, 로컬 .env 설정에 따라 visual 스냅샷이 사람마다 달라진다.
VITE_API_BASE_URL=
