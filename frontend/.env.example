# FastAPI base URL. Keep empty when the frontend is served behind the same origin proxy.
VITE_API_BASE_URL=http://localhost:8000

# Use the in-browser MSW API during frontend-only development.
VITE_USE_MOCKS=false
